import React from 'react';

const InitProvider = ({ children }) => {
  // Task 1: Check Authentication
  // Task 2: Fetch data and set into Store
  // Task 3:
  return <div>{children}</div>;
};

export default InitProvider;
